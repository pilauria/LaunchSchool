const advice = 'Few things in life are as important as house training your pet dinosaur.';

console.log(advice.replace('important', 'urgent')); // => replace the first occurence;
console.log(advice.replaceAll('important', 'urgent')); // => replace all the occurence;
