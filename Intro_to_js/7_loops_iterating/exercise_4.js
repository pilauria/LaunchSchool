for (let i = 0; i < 5; ) {
  console.log(i += 1);
}

/*
No errors, because al the three component of the for loop are optional. Although we omit the "next value" component on line 1, we put it in the  line 2. 
*/
 
