function scream(words) {
  return words + "!!!!";
}

scream("Yipeee");

// The function doesn't log anything. Return is a statement that allows the function to output the value back to where is called, but it doesn't do anything with it.
