function scream(words) {
  words = words + "!!!!";
  return;
  console.log(words);
}

scream("Yipeee");

// the function do not returns anything, because on line 3 the "return" termitates the function.
